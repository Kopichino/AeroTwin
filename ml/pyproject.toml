[project]
name = "at-ml"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = ["at-core", "at-data", "numpy>=2.0", "pandas>=2.2", "torch>=2.5"]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/at_ml"]
