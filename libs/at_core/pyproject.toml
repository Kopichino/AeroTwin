[project]
name = "at-core"
version = "0.1.0"
description = "AeroTwin pure domain kernel"
requires-python = ">=3.12"
dependencies = []

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/at_core"]

[tool.hatch.build.targets.wheel.force-include]
