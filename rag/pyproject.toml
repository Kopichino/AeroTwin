[project]
name = "at-rag"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = ["at-core", "numpy>=2.0", "sentence-transformers>=5.0", "chromadb>=1.0"]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/at_rag"]
