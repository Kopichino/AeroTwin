"""Request and response models for the HTTP surface.

These are wire contracts, versioned with the API. They are deliberately separate
from the domain model so an internal refactor cannot silently break clients.
"""
